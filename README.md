# 🌤️ Weather App

Веб-приложение для получения прогноза погоды по городам с использованием OpenWeatherMap API.

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![Flask](https://img.shields.io/badge/Flask-3.0.0-green.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

## 📋 Описание

Weather App - это современное веб-приложение на Flask, которое позволяет получать актуальную информацию о погоде в любом городе мира. Приложение имеет красивый интерфейс с анимированным фоном, поддерживает несколько языков и сохраняет историю запросов.

## ✨ Основные функции

- 🌍 **Поиск погоды** по названию города
- 🌡️ **Подробная информация**: температура, ощущаемая температура, влажность, скорость ветра
- 🎨 **Современный UI** с эффектом стекла (glassmorphism) и анимированным фоном
- 🗄️ **История запросов** с хранением в SQLite
- 🌐 **Многоязычность** (Русский/English)
- 📍 **Автоопределение города** по IP-адресу
- 🎭 **Иконки погоды** от OpenWeatherMap
- ⚠️ **Обработка ошибок** (город не найден, нет соединения и т.д.)

## 🚀 Установка и запуск

### Требования

- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/yourusername/weather-app.git
cd weather-app
```

### Шаг 2: Создание виртуального окружения

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Получение API ключа

1. Зарегистрируйтесь на [OpenWeatherMap](https://openweathermap.org/api)
2. Получите бесплатный API ключ
3. Откройте файл `config.py` и замените `YOUR_API_KEY_HERE` на ваш ключ:

```python
OPENWEATHER_API_KEY = 'ваш_api_ключ_здесь'
```

**Альтернативный способ (рекомендуется для продакшена):**

Создайте переменную окружения:

**Windows:**
```bash
set OPENWEATHER_API_KEY=ваш_api_ключ_здесь
```

**Linux/macOS:**
```bash
export OPENWEATHER_API_KEY=ваш_api_ключ_здесь
```

### Шаг 5: Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: [http://localhost:5000](http://localhost:5000)

## 📁 Структура проекта

```
WeatherApp/
│
├── app.py                  # Главный файл Flask приложения
├── config.py              # Конфигурация приложения
├── database.py            # Работа с SQLite базой данных
├── translations.py        # Переводы для многоязычности
├── requirements.txt       # Зависимости Python
├── README.md             # Документация
│
├── templates/            # HTML шаблоны
│   ├── base.html        # Базовый шаблон
│   └── index.html       # Главная страница
│
├── static/              # Статические файлы
│   ├── css/
│   │   └── style.css   # Стили приложения
│   └── js/
│       └── app.js      # JavaScript логика
│
└── weather_history.db  # SQLite база данных (создается автоматически)
```

## 🎨 Технологии

### Backend
- **Flask** - веб-фреймворк
- **SQLite** - база данных для истории
- **Requests** - HTTP запросы к API

### Frontend
- **HTML5** - разметка
- **CSS3** - стилизация с glassmorphism эффектами
- **Bootstrap 5** - адаптивный дизайн
- **JavaScript (Vanilla)** - интерактивность
- **Bootstrap Icons** - иконки

### API
- **OpenWeatherMap API** - данные о погоде
- **ipinfo.io** - геолокация по IP

## 🔧 Конфигурация

Файл `config.py` содержит все настройки приложения:

```python
class Config:
    SECRET_KEY = 'ваш-секретный-ключ'
    OPENWEATHER_API_KEY = 'ваш-api-ключ'
    DATABASE_PATH = 'weather_history.db'
    SUPPORTED_LANGUAGES = ['ru', 'en']
    DEFAULT_LANGUAGE = 'ru'
```

## 📝 Использование

1. **Поиск погоды**: Введите название города и нажмите "Показать погоду"
2. **Автоопределение**: Нажмите "Определить мой город" для автоматического определения
3. **История**: Кликните на любой город из истории для повторного просмотра
4. **Смена языка**: Используйте кнопки RU/EN в верхнем правом углу

## 🌐 API Endpoints

- `GET /` - Главная страница
- `POST /weather` - Получение данных о погоде
- `POST /autodetect` - Автоопределение города по IP
- `GET /history` - История запросов
- `GET /set_language/<lang>` - Установка языка

## 🐛 Обработка ошибок

Приложение обрабатывает следующие ошибки:

- ❌ Город не найден
- ❌ Неверный API ключ
- ❌ Нет соединения с интернетом
- ❌ Общие ошибки сервера

## 📱 Адаптивность

Приложение полностью адаптировано для всех устройств:
- 💻 Десктоп
- 📱 Мобильные устройства
- 📱 Планшеты

## 🔐 Безопасность

- Использование переменных окружения для API ключей
- Защита от SQL-инъекций через параметризованные запросы
- Валидация пользовательского ввода

## 🚀 Развертывание

### Heroku

1. Создайте `Procfile`:
```
web: gunicorn app:app
```

2. Добавьте `gunicorn` в `requirements.txt`:
```bash
pip install gunicorn
pip freeze > requirements.txt
```

3. Установите переменные окружения в Heroku:
```bash
heroku config:set OPENWEATHER_API_KEY=ваш_ключ
```

### Docker

Создайте `Dockerfile`:
```dockerfile
FROM python:3.9-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "app.py"]
```

## 🤝 Вклад в проект

Если вы хотите внести вклад в проект:

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле `LICENSE`.

## 👨‍💻 Автор

**Daniil**

- GitHub: [@madaodlb](https://github.com/dettline1)

## 🙏 Благодарности

- [OpenWeatherMap](https://openweathermap.org/) за предоставление API
- [Bootstrap](https://getbootstrap.com/) за UI компоненты
- [ipinfo.io](https://ipinfo.io/) за геолокацию

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.

---

⭐ Если вам понравился проект, поставьте звезду на GitHub!

**Weather App by Daniil** © 2025

