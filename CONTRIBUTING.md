# 🤝 Руководство по внесению вклада

Спасибо за интерес к проекту Weather App! Мы рады любому вкладу в развитие приложения.

## 📋 Как внести вклад

### 1. Сообщить об ошибке (Bug Report)

Если вы нашли ошибку:

1. Проверьте, что такой issue еще не создан
2. Создайте новый issue с меткой `bug`
3. Опишите проблему:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Окружение (ОС, версия Python, браузер)

### 2. Предложить новую функцию (Feature Request)

Есть идея для улучшения?

1. Создайте issue с меткой `enhancement`
2. Подробно опишите предлагаемую функцию
3. Объясните, почему это будет полезно
4. Приведите примеры использования

### 3. Внести код (Pull Request)

#### Подготовка

```bash
# 1. Форкните репозиторий через GitHub

# 2. Клонируйте ваш форк
git clone https://github.com/ваш-username/weather-app.git
cd weather-app

# 3. Добавьте upstream remote
git remote add upstream https://github.com/original-owner/weather-app.git

# 4. Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# 5. Установите зависимости
pip install -r requirements.txt
```

#### Разработка

```bash
# 1. Создайте новую ветку
git checkout -b feature/amazing-feature

# 2. Внесите изменения
# - Следуйте стилю кода проекта
# - Добавьте комментарии
# - Обновите документацию при необходимости

# 3. Протестируйте изменения
python test_api.py

# 4. Закоммитьте
git add .
git commit -m "Add: Amazing feature"

# 5. Запушьте в ваш форк
git push origin feature/amazing-feature
```

#### Создание Pull Request

1. Перейдите на GitHub в ваш форк
2. Нажмите "New Pull Request"
3. Заполните шаблон PR:
   - Описание изменений
   - Связанные issues
   - Скриншоты (для UI изменений)
   - Чеклист тестирования

## 📝 Стандарты кода

### Python (PEP 8)

```python
# Хорошо
def get_weather_data(city, lang='ru'):
    """
    Получение данных о погоде
    
    Args:
        city: название города
        lang: язык ответа
    
    Returns:
        dict: данные о погоде
    """
    pass

# Плохо
def getWeatherData(city,lang='ru'):
    pass
```

### JavaScript (ES6+)

```javascript
// Хорошо
async function searchWeather() {
    const city = document.getElementById('cityInput').value;
    try {
        const response = await fetch('/weather', {
            method: 'POST',
            body: JSON.stringify({ city })
        });
        // ...
    } catch (error) {
        console.error(error);
    }
}

// Плохо
function searchWeather() {
    var city = document.getElementById('cityInput').value;
    fetch('/weather', {
        method: 'POST',
        body: JSON.stringify({ city: city })
    }).then(function(response) {
        // ...
    });
}
```

### CSS

```css
/* Хорошо - BEM naming */
.weather-card {
    background: rgba(255, 255, 255, 0.1);
}

.weather-card__title {
    font-size: 1.5rem;
}

.weather-card__title--highlighted {
    color: gold;
}

/* Плохо */
.weatherCard {
    background: rgba(255, 255, 255, 0.1);
}
```

## 🧪 Тестирование

Перед отправкой PR убедитесь, что:

- [ ] Приложение запускается без ошибок
- [ ] Все тесты проходят (`python test_api.py`)
- [ ] Нет ошибок в консоли браузера
- [ ] UI корректно отображается на разных экранах
- [ ] Код соответствует стандартам проекта

## 📚 Документация

При добавлении новых функций обновите:

- `README.md` - общее описание
- `FEATURES.md` - список функций
- `API_DOCUMENTATION.md` - API endpoints
- Docstrings в коде

## 🏷️ Commit Messages

Используйте префиксы:

- `Add:` - новая функция
- `Fix:` - исправление ошибки
- `Update:` - обновление существующего кода
- `Refactor:` - рефакторинг без изменения функциональности
- `Docs:` - изменения в документации
- `Style:` - форматирование, отступы
- `Test:` - добавление тестов

Примеры:
```
Add: City autocomplete feature
Fix: Database connection error on startup
Update: Bootstrap version to 5.3.0
Docs: Add API examples to README
```

## 🎯 Приоритетные задачи

Мы особенно приветствуем вклад в:

1. **Тесты** - Unit tests, Integration tests
2. **Документация** - Примеры, tutorials
3. **Локализация** - Новые языки
4. **Accessibility** - A11y improvements
5. **Performance** - Оптимизация кода

## 📬 Вопросы?

Есть вопросы? Создайте issue с меткой `question` или напишите автору.

---

**Спасибо за ваш вклад!** 🙏

Weather App Community

